info:
  name: get categories
  type: http
  seq: 6

http:
  method: GET
  url: localhost:3000/api/categories
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
