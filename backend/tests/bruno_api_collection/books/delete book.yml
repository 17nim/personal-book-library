info:
  name: delete book
  type: http
  seq: 7

http:
  method: DELETE
  url: localhost:3000/api/books/6
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
